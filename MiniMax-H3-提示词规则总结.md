# MiniMax H3 提示词规则总结

> 来源：《🐚MiniMax H3 模型 - 使用手册》（含 goodcase 与实战案例）
> + 官方开源提示词技能 [MiniMax-AI/MiniMax-H3](https://github.com/MiniMax-AI/MiniMax-H3)（已存档于本目录 `official_h3_skill/`）
> 用途：把大白话需求改写成符合 H3 规则的提示词时的对照手册

---

## 一、模型基础信息（写提示词前要知道的硬约束）

| 项目 | 规则 |
|---|---|
| 输出时长 | 4–15 秒 |
| 宽高比 | 文生/全能参考模式：21:9、16:9、4:3、1:1、3:4、9:16；首/尾帧模式跟随输入图片原始比例 |
| 帧率 / 声音 | 24 FPS；所有结果原生带声音、双声道 |
| 分辨率 | 768p 或 1440p（官方推荐 1440p） |
| 提示词上限 | **不超过 7000 字符** |
| 输入上限（全能参考） | 图片 ≤9 张、视频 ≤3 段（每段 2–15 秒、总长 ≤15 秒）、音频 ≤3 段（须搭配图片/视频），混合文件总数 ≤12 |
| 语言 | 支持多语言提示词，TTS 覆盖中英日韩法德西等 11+ 种语言 |

---

## 二、三种生成模式，提示词写法不同

### 1. 纯文生视频（无参考素材）
- 描述必须**更具体**：主体外观、场景细节、动作、风格都要写清。
- 推荐分层写法：**大全景交代空间 + 中景承载动作 + 特写强调细节**。

### 2. 图生视频（上传 1–2 张图）
- 只传 1 张：必须说清是**首帧**（开头画面）还是**尾帧**（结尾画面）。
- 传 2 张（首+尾帧）：H3 **不会自动加切镜**，只补两帧之间的动作、光影、声音；要写成自然衔接、不切镜。

### 3. 多模态参考（图片+视频+音频混合）
- 每个素材都要**写明角色/用途**，并用 @编号 指代。
- 例：`@图片1 是人物参考（锁脸），@视频1 是动作参考，@音频1 是情绪参考`。

---

## 三、核心公式（最重要）

```
完整提示词 = 参考素材说明 + 核心创意 + 画面过程说明
```

### 要素 1：参考素材说明
- 写清素材编号：按上传顺序 `@图片1`、`@音频2`、`@视频3`。
- 写清每个素材用途，常见类型：
  - 人物参考（锁脸/形象）、物体参考（锁物体）、场景参考（锁场景）
  - 首帧/尾帧/关键帧（明确说是首帧还是尾帧）
  - 音色参考（锁音色）、音频复用（直接复用整段音频）、音频部分复用
  - 动作参考（锁动作）、运镜参考（锁运镜）、构图参考
  - 风格参考（按图片风格生成）、故事版（按分镜生成镜头内容）
  - 视频编辑（对视频增删改）
- 特别想要保持的特征，**明确写出来**，一致性更好。
- 音频复刻人声/歌词时，强烈建议把歌词原文写进提示词。
- 没有上传素材时，这一段整段跳过。

### 要素 2：核心创意（一句话锁全片）
必须包含：
- **主体**：谁/什么
- **地点**：在哪里
- **事件**：在做什么
- **题材/风格**：写实/动画/电影感/广告片/纪录片/赛博朋克等调性
- **特殊运镜**：航拍/一镜到底/慢动作等

运镜与切镜注意：
- 默认会切镜；不想切镜要写明「一镜到底」。
- 环绕运镜建议写 `truck left + pan right` 或 `truck right + pan left`，不要只写「环绕运镜」。
- 切镜风格可指定：普通切镜（cut）/ 叠化（fade）/ 卡点切镜 / 快切。

示例：`一位穿汉服的年轻女子（@图片1）在樱花纷飞的庭院里舞剑，古典国风，电影质感，一镜到底。`

### 要素 3：画面过程说明（按时间轴/故事线分段）
每个分镜/时间段写两部分：

**「想要」**＝景别 + 内容 + 运镜 + 动作 + 台词 + 音效
- 以切镜（shot）作为时间戳分段；每个 shot 内写清景别、内容、内部运镜、台词、音效。
- **台词长短要和镜头长短对齐**（口型问题大多出在这里）。
- 一句台词跨 shot 时，写明「接着上个 shot 继续说」（支持 J-cut / L-cut）。
- 需要出现的具体文字/Logo/标题/按钮文案，**必须写出原文**，例如：`画面中出现英文：“H3”`。

**「不想要」**＝明确排除项
- 不要 BGM 时直接写：`非叙事性音乐：N/A` 或 `"non_diegetic_music": N/A`。
- 其他不要的内容（字幕、水印、现代元素、血腥等）逐条列出。

写作纪律：
- **少写比喻，多写看得见的画面**；H3 擅长具体直接的可视化指令，不擅长「意会」。
- 台词必须给出原文，如：`女子说：“你来了，剑等你好久了。”`

---

## 四、镜头拆分建议

- 台词长度与 shot 时长要匹配：**不要出现 3 秒镜头说一大段话**。
- 跨 shot 台词：明确写出这句话跨了哪几个 shot。
- 说话人区分：写清是画面内角色还是画外音（V.O.）；画内外转换可明确写出。
- 切镜一致性：切镜时写明「切到什么景别、主体是之前哪个角色」，有助跨镜头一致性。

---

## 五、容易踩的坑（自查清单）

| 常见问题 | 改法 |
|---|---|
| 只写一段话没分段 | 按「素材说明 + 核心创意 + 画面过程」三段拆开 |
| 上传了素材但没说用途 | 补一句 `@图片1 是 XX 参考` |
| 想要音乐又说「不要 BGM」 | 矛盾，删一个或按场景拆分写 |
| 想一镜到底却写了很多分镜 | 保持一段情节描述，删掉【镜头 N】结构 |
| 想要主角脸一致但没传图 | 必须上传人物参考图并标注「人物参考」 |
| 提示词太短 | 至少写出：主体外观 + 场景细节 + 动作 + 风格 |

---

## 六、从 goodcase 提炼的风格化写法（进阶）

实战案例中高质量提示词的共同特征：

1. **开头声明基础参数**：时长 + 画幅先写死，如 `15 秒、16:9 横版`。
2. **给参考图分配职责**：`图1是整体氛围、风格参考，图2是人物参考`，并枚举需要保持的特征（发型、服装、气质…）。
3. **分镜用时间戳或 Shot 编号**：如 `[0-2秒]`、`Shot 1 — 面部极近特写`。
4. **文字类内容必须给原文**：字幕、Logo、按钮、标题逐字写清，并规定出现方式（渐入/弹出/字距展开）与时长。
5. **声音单独写一段**：BGM 构成、节奏演进（如「前 2 秒低频建立悬疑，第 3 秒进入鼓点」）、环境音、人声内容。
6. **负面清单成段给**：`不要字幕、不要水印、不要现代元素、不要动画感、不要廉价恐怖`。
7. **编辑类任务强调「保持不变」**：替换背景时写明「镜头运动、物体位置、空间遮挡、光照 100% 不变，无缝融合」。
8. **复杂片子的「整体要求补充」段**：把贯穿全片的规则分类列出（影像风格、文字包装、律动规则、场景/景别切换、人物规则、表演要求、禁止项）。

---

## 七、推荐模板（可直接套用）

```text
【参考素材说明】
@图片1：人物参考（锁这位角色的脸/形象），需保持：外貌特征逐条列出。
@图片2：场景参考。
@视频1：动作参考（用里面的 XX 动作）。
@音频1：音色参考 / 情绪参考，具体歌词：“原文”。
（无素材则整段写：无参考素材，纯文生视频。）

【核心创意】
一句话：主体 + 地点 + 事件 + 风格 + 特殊运镜。例如：
15 秒，16:9 横版，一位穿汉服的女子（@图片1）在樱花庭院（@图片2）舞剑，古典国风，电影质感，一镜到底。

【画面过程描述】
0–3 秒：全景，女子从画面左侧缓步走入庭院，背景虚化，无对白，只有脚步声。
3–8 秒：切到女子中景，她拔出长剑缓缓起势，樱花瓣飘落，镜头推进。
8–12 秒：切到特写，剑光一闪，慢动作，樱花被剑气激散。
...

【整体要求补充】（可选，复杂片子用）
▍影像风格（贯穿全片）：...
▍文字包装（贯穿全片）：...
▍声音设计（贯穿全片）：...
▍禁止项（贯穿全片）：...
非叙事性音乐：N/A（如不要 BGM）
```

---

## 八、给 H3 写提示词时的「大白话→规范」转换要点

用户说「我要一个酷炫的视频」→ 转成：主体是什么 + 什么场景 + 做什么动作 + 什么风格（赛博朋克/胶片/广告感…）+ 时长画幅 + 是否切镜。

用户说「参考这张图」→ 转成：`@图片1 是 XX 参考`，并写明保持什么、不复制什么。

用户说「加个特效」→ 转成：特效的**具体样子**（颜色、形态、出现时机、与主体的关系），如「橙黄色手绘涂鸦从火花变成明亮光芒」。

用户说「不要背景音乐」→ 直接写：`非叙事性音乐：N/A`。

---

# 九、官方 GitHub 提示词格式（h3-prompt-writing 技能）

> 来源：`https://github.com/MiniMax-AI/MiniMax-H3/tree/main/skills/h3-prompt-writing`
> 已存档：本目录 `official_h3_skill/SKILL.md`、`official_h3_skill/references/base-en.txt`、`official_h3_skill/references/ref-en.txt`

**和手册的关系**：手册的中文三段式是「创作层面的写法指南」；官方技能是「最终提示词的标准输出格式」，字段更精确、更接近 H3 实际解析的结构。两者互补：先用手册想创意，再用官方格式落笔。

## 1. 五种生成模式

| 模式 | 全称 | 含义 |
|---|---|---|
| T2VA | Text-to-Video | 纯文生视频 |
| I2VA | Image-to-Video | 首帧图生视频，从首帧向前发展 |
| FL2VA | First & Last-to-Video | 首+尾帧，补两帧之间的连续路径 |
| L2VA | Last-to-Video | 尾帧，从推测的前序状态收敛到尾帧 |
| Ref2VA | Full-Reference | 全能参考（多素材融合 / 编辑） |

## 2. 基础模式（T2VA / I2VA / FL2VA / L2VA）输出结构

### 图片对齐指令（有图才写，必须是第一行，后空一行）

- I2VA：
  `For the target video, at 0.00 seconds into the target video, <Picture 1> (from [Shot 1]) is fully referenced.`
- FL2VA：
  `How the reference pictures align with the target video — Picture 1 (from Shot 1) aligns with the 0.00-second mark of the target video; Picture 2 (from Shot N) aligns with the S.SS-second mark of the target video.`
- L2VA：
  `How the reference pictures align with the target video — <Picture 1> (from [Shot N]) aligns with the S.SS-second mark of the target video.`

### 三个核心字段（固定顺序）

```text
integrated_multimodal_description: [Shot 1] ...
overall_soundscape: ...
non_diegetic_music: ...
```

- `integrated_multimodal_description`：主文，沿时间轴写画面、动作、镜头、说话人、台词、画面内声音。
- `overall_soundscape`：1–4 句英文，概括全片环境音和物理音效（风声、雨声、脚步声、衣物摩擦、撞击、呼吸等）；对白/唱歌/画面内音乐不写在这里；全片完全无声才写 N/A。
- `non_diegetic_music`：1–3 句英文，描述只有观众能听到的配乐，写器乐、速度、节奏、动态变化，不写抽象情绪词；没有则写 N/A。

## 3. Shot 与时间戳

- `[Shot 1]` 不带时间戳；后续 `[Shot N] At MM:SS.mmm`，时间严格递增且在视频时长内。
- 普通切镜：`the camera cuts to / the shot cuts to / the shot transitions to / the shot changes to / the shot switches to`；用户明确要求时才用 cross-dissolve / fade / wipe。
- 切镜要带来新信息（主体、空间、状态、视角、时间的变化）；只是距离或角度微调时，优先用运镜而不是切镜。

## 4. 运镜公式：类型 + 幅度 + 速度

- 类型：Zoom In/Out、Push In/Pull Out、Pan Left/Right、Truck Left/Right、Tilt Up/Down、Pedestal Up/Down、Arc Shot、Tracking Shot、Static Shot、Shake Slightly/Strongly、POV、Roll Clockwise/Counterclockwise
- 幅度：`with small amplitude` / `with large amplitude`
- 速度：`at slow speed` / `at fast speed`
- 写成自然英文句，不要堆标签：
  `The camera pushes in with small amplitude at slow speed toward the folded letter in her hands.`

## 5. 说话人与台词

- 说话人统一编号 `(S1)` `(S2)`，跨镜头保持不变；多人同时说用 `(S1,S2)`；不发声的角色不编号。
- 首次出现时给足身份信息：角色类型、年龄、性别、画内/画外、音色、语速、口音等。
- 台词格式：`<d>[Language] 原文</d>`，**逐字保留用户原话，不翻译不改写**。
- 画外音：`says in an off-screen voiceover: <d>...</d>`，后面必须写明对应角色嘴巴不动（lips remain completely closed）。
- 跨镜头台词：两端都用 `<scenetrans>`，并写明音频跨切镜延续（如 continues seamlessly across the cut）。
- 视频结尾被截断的台词：用 `<cutoff>`。
- 画面内可见文字（招牌、字幕、霓虹灯等）：英文双引号 `"..."` 原样保留，不翻译。

## 6. 全能参考模式（Ref2VA）六段结构（固定顺序）

```text
subject_definitions:
summary:
retention_analysis:
detailed_description:
overall_soundscape:
non_diegetic_music:
```

### 标签类型

| 标签 | 含义 |
|---|---|
| `<Subject N>` | 可复用的可见内容：人物、动物、物体、场景、服装、道具、界面、特效、风格、动作、表情、姿势 |
| `<Picture N>` | 作为具体帧/构图锚点的参考图（首帧、关键帧、尾帧、故事板） |
| `<Video N>` | 被直接编辑、续写、或提供镜头/节奏/时间结构的原视频 |
| `<Audio N>` | 被复制或参考的音频信号 |

### summary 任务类型（可 ` + ` 组合，不重复）

`keyframe completion`（图作帧锚点）/ `reference generation`（素材只作生成参考）/ `video editing`（直接改原视频）/ `video continuation`（续写原视频）/ `audio reuse`（整段或部分复制音频）/ `audio reference`（只参考风格/音色/节奏）

示例：`[video continuation + keyframe completion]`

### retention_analysis 关系标记

- 可见内容：`fully_preserved` / `partially_preserved` / `attribute_transfer`（特征转移到另一个主体）/ `weak_reference`
- 音频：`fully_copy` / `partially_copy` / `reference` / `weak_reference`

### detailed_description 要点

- 生成类任务通常 350–500 英文词；对话密集时以完整放下台词时间轴为准，不硬凑字数。
- 先写 1–2 句英文风格总起句（如 cinematic / live-action / 2D-animated / 3D CG / claymation / watercolor），再进入 `[Shot 1]`。
- 每镜头写清：当前构图、主体外观与位置、环境与光照、动作与状态变化、运镜、当前声音、参考内容实际出现的位置。
- 参考标签在其首次出现处插入，之后沿用；不要定义后不用（悬空标签）。

## 7. 输出规则（写提示词时自检）

- 六个部分用英文写；对话、歌词、画面可见文字保留原语言。
- 不要写成剧情梗概，不要留悬空参考标签，时间线必须匹配请求的时长。
- 只描述看得见听得见的东西；不要抽象比喻和情绪词堆砌。
