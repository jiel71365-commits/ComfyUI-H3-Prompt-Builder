# MiniMax H3 仓库 8 个风格化技能分析

> 来源：`github.com/MiniMax-AI/MiniMax-H3/skills/`（已存档于本目录 `official_h3_skill/style_skills/`）
> 结论先行：**这些技能不是模型自带、也不是 ComfyUI 插件，而是「AI Agent 技能」（SKILL.md 指令集）**，用来指挥智能体按流程产出 H3 提示词 / 参考图 / 成片。它们输出的最终 H3 提示词可以直接喂给 ComfyUI 的 MiniMax H3 节点。

---

## 总览表

| 技能 | 用途 | 产出 | 视频模型策略 | 与 ComfyUI 的关系 |
|---|---|---|---|---|
| minimalist-product-ad-generator | 极简 Apple 风电商产品广告 | 3 张独立锚点图 + 节拍分镜表 + H3 视频 + BGM | 默认 H3，Seedance 兜底 | 产出 prompt 填 T2V/I2V 节点 |
| 3d-animation-short-generator | 完整风格化 3D 动画短片（故事→成片） | 角色卡/场景卡/六列镜头表/文字分镜/逐镜头视频/合片 | 默认 H3，Seedance 2.0 兜底 | 每镜头 prompt 填节点，外部合片 |
| papercraft-stop-motion-explainer | 纸艺定格动画科普/教育解说 | 风格规则、角色场景设计、提示词库、分镜、声音方案、负面清单 | 默认 H3 | 产出 prompt 填节点 |
| paper-collage-explainer-generator | 半色调纸拼贴动画（观点/抽象概念） | 定帧图 + 逐段 4s 拼贴动画 + 拼贴 SFX | 默认 H3 | 产出 prompt 填节点 |
| music-video-subtitle-generator（目录名 mv-subtitle-skill-confirmed） | MV + 歌词字体排版（Trap/Dark-pop 等） | MV 概念、分镜头 prompt、歌词文字计划、拼接方案 | H3 或按需路由 | 产出 prompt 填节点 |
| brand-promo-video-generator | 品牌/产品/网站宣传短片 | 品牌事实表、素材来源清单、节拍计划、成片 | 默认 H3 原生音频 | 产出 prompt 填节点 |
| co-op-game-intro-generator | 双人联机游戏菜单/片头 | 1 张确认图 + H3 视频（自带两个模板） | H3 | 产出 prompt 填节点 |
| handdrawn-live-video-generator | 手绘发光动画+实拍融合短片 | 直接用用户语言输出 15s 16:9 prompt | 推荐 H3 | 产出 prompt 填节点 |

---

## 逐个要点

### 1. minimalist-product-ad-generator（极简产品广告）
- 起点门（start gate）一次确认：产品素材、多色款、时长（默认 10s）、画幅、Apple 风格模板、画面文案、模型策略（默认 H3，不询问）。
- 关键原则：**3 张独立锚点图**（主视觉/材质细节/结尾文案构图），不用四宫格拼板（视频模型会复刻拼板布局）；不用原产品图直出视频。
- 文案铁律：3–5 个英文单词、单行、最多 32 字符；同一时刻只允许一行文案；双色规则（前半黑/白 + 后半用产品真实主色）；禁止上下两行、禁止副标题位。
- 产品本体颜色是硬保真约束：只换背景/光环境，不改产品颜色。
- 节拍分镜表（时间/镜头/视觉主导/文案/转场/节奏意图）是执行核心；视频 prompt 必须逐字包含每条文案、出现时间和动效。
- 音频：默认 H3 原生音频；不满意再用 music-2.6 生成 BGM，做卡点剪辑后替换。

### 2. 3d-animation-short-generator（3D 动画短片）
- 完整流水线：项目简报 → 故事大纲（8 拍因果）→ 角色卡（带标签的 16:9 参考卡）→ 场景卡（纯环境，禁止人物）→ **六列标准镜头表**（Shot ID/连续性交接/空间+身份参考锚点/Hook 类型/逐秒指令/音频轨道）→ 文字分镜（默认；铅笔分镜可选）→ 单镜头视频 → 合片 + BGM。
- 每个关键节点都有确认卡（choice card）；镜头表有专门自检门（Step 5.5）。
- 视觉风格默认锁死 Pixar 风 3D（C4D+Octane 质感、Q 版比例、挤压拉伸表演），负向禁止写实真人/2D 平涂/塑料皮肤。
- 默认 H3，Seedance 2.0 作为高难度表演镜头的兜底；允许逐镜头混合模型（表里标注）。
- 时长通常 30–60s → 逐镜头生成再拼接，不依赖单次 15s。

### 3. papercraft-stop-motion-explainer（纸艺定格解说）
- 科普/教育话题 → 纸艺定格动画生产包（18 步）：理解输入 → 风格 DNA → 3–5 个创意方向 → 纸角色/场景/分层舞台/道具资产 → 预览图 → 单图/图序列/5s 图生视频提示词 → 分镜 → 剪辑节奏/运镜/转场/声音 → 配音 → 负面提示词 → 检查清单。
- 硬性风格：分层卡纸、可见纸纤维/折痕/切边/厚度、真实投影、前景遮挡、视差、定格运动（阶梯式动作+小停顿+回弹），禁止顺滑 CG/塑料/液体变形。
- 分镜时长建议：15s≈4 镜、30s≈5–6 镜、60s≈7–9 镜；每镜只讲一个知识点。
- 有「轻量旁路」：只要单个资产（如 5s 图生视频 prompt）时跳走完整流程。
- 默认 H3；长文档输出到 canvas，聊天里只给摘要。

### 4. paper-collage-explainer-generator（纸拼贴解说）
- 一句话/一个观点 → 半色调纸拼贴视觉隐喻动画。默认 16:9、每段约 4 秒。
- 音频政策很有特点：**默认只保留拼贴 SFX（纸滑/弹入/按平/轻响），不加 BGM、不加旁白、不加字幕**，除非用户明确要求。
- 两道 Gate：生产计划文档批准 → 定帧图批准 → 才生成逐段动画；定帧图 = 动画的最终帧锚点，运动 = 从色纸场逐步拼装到位（出现→轻弹→按平→停顿→锁定）。
- 视觉语言：平坦大胆色场 + 黑白半色调剪贴 + 少量彩色卡纸 + 奶油色描边 + 柔和纸影。
- 默认 H3，若生成音频里混入无关语音/音乐则替换或重生成。

### 5. music-video-subtitle-generator（MV 字幕 / 音乐视觉）
- 音乐 + 歌词 + 字体排版 + 节奏 + 镜头语言一体设计。前置锁定：画幅（9:16/16:9/1:1/21:9 等）、时长、音乐窗口、歌词（用户给的歌词锁定，禁止改写；没有歌词则先生成原创歌词并锁定）。
- **>15s 自动进入多镜头拼接协议**：锁一条全局 Master Audio → 把 30s 拆成 4–8 个 2–5s 镜头并映射歌词时间戳 → 镜头间用尾帧/首帧续接或卡点硬切 → 剪辑按节拍网格对齐。
- 排版规则：字是空间里的动态图形层，不是字幕条；可被人物遮挡但不得遮眼/关键表情；有演唱时画面文字必须逐字等于演唱歌词；每镜头只有一个主文字事件。
- 剪辑语法：只允许硬切（hard cut），禁淡入淡出；hi-hat/snare/808 各自触发对应画面动效。
- 输出「完整 MV Prompt」到 canvas，包含每个镜头的 Vocal Line / Typography / Visual & Action / Camera / Transition。

### 6. brand-promo-video-generator（品牌宣传片）
- 品牌/产品/网站/App 的 15–30s 宣传短片。**强调查证与来源**：logo、字体、颜色、产品信息必须来自官方或用户授权素材；不允许 AI 复刻 logo/品牌标识；产出「来源清单（provenance manifest）」。
- 流程：素材采集 → 品牌事实表 → 来源清单 → 故事主轴（AI/SaaS、实体产品、服务、形象型四种模板）→ 帧级节拍计划（15s≈5–8 拍）→ 运动语言 → 硬确认门 → 生成 → 校验 → 交付。
- 默认 H3 原生音频（generate_audio=true），只有需要可控旁白/独立 BGM 时才单独生成音频；字幕默认不加。
- 不允许编造产品数据/口号；没有授权素材就停下来要原文件，绝不「美化仿冒品」。

### 7. co-op-game-intro-generator（联机游戏片头）
- 双人游戏主菜单/片头动画：先收集风格、两位玩家名、游戏标题、可选角色图 → 用固定菜单框架生成 **1 张确认图**（框架固定：左上玩家卡、右侧竖排菜单、底部警示胶带；风格动态填充）→ 用户批准后才填 H3 视频 prompt 生成。
- 自带两个强制模板：`references/h3-confirmation-image-template.md`（确认图 prompt）和 `references/h3-video-prompt-template.md`（最终 H3 视频 prompt）——已存档，可直接当 H3 prompt 参考。
- 角色参考图只提取身份锚点（脸型轮廓、发型、眼镜、比例），按选定风格重绘，不继承原图写实质感。

### 8. handdrawn-live-video-generator（手绘实拍融合）
- 15s、16:9、手绘发光动画与实拍空间融合。**这个技能本质就是「写 prompt」**，和你做提示词生成的项目最接近。
- 输出语言跟随用户输入语言（中文输入→中文 prompt；最终结构固定：开头句 → 实拍空间与手机质感 → 0–3s 接触 → 3–6 → 6–10 → 10–13 → 13–15 → 手绘质感 → 相机慢半拍 → 禁止项 → 环境音）。
- 每次创作全新组合（禁止复用示例里的场景/变形链/角色），核心结构必须保留：动画与真实手/物体接触、同一存在连续变形、相机总是慢半拍、结尾空间级变形+可爱余韵。
- 0–3 秒必须有实拍手与手绘实体的清晰接触；禁止 3DCG/恐怖化/跳吓。
- 写完后推荐用 H3 生成，确认后才进入生成。

---

## 共同特征

1. 视频模型默认全部指向 **MiniMax-H3**；个别技能把 Seedance 2.0 当兜底。
2. 全部是「带确认门的流水线」：start gate / choice card / Gate 1 / Gate 2，逐步确认再生成，不一次跑完。
3. 都不是单个 prompt 模板，而是「计划 → 参考图/锚点 → 分镜 → 最终 H3 prompt → 生成 → 校验」的完整流程；对 H3 有用的核心沉淀是**最终那一段 H3 prompt**。
4. 大量技能是 MiniMax Hub 适配版（`exported-by: MiniMax-hub`，工具列表为 `hub_generate_image/video/audio` 等），在 Hub 的 Agent 环境里可直接跑；在其他 Agent（如 Codex）里由等效工具替代，纯 prompt 部分通用。
5. 长产出（计划/分镜/检查清单）要求写到 canvas 文档，聊天里只给摘要和下一步。

---

## 这些技能是不是「自带的」？怎么用？

**不是模型自带的，也不是 ComfyUI 内置的。**它们是给「AI 智能体」用的指令包（SKILL.md），安装后智能体才知道怎么按流程干活。和 ComfyUI 的关系是「前端给料」：技能最终产出的 H3 提示词文本（+参考图），由你把它们填进 ComfyUI 的 MiniMax H3 节点。

### 安装/使用方式

1. **官方 skills CLI（通用）**：
   ```bash
   npx skills add https://github.com/MiniMax-AI/MiniMax-H3 --list          # 查看全部
   npx skills add https://github.com/MiniMax-AI/MiniMax-H3 --skill '*'     # 装全部 9 个
   npx skills add https://github.com/MiniMax-AI/MiniMax-H3 --skill minimalist-product-ad-generator
   ```
2. **MiniMax Hub**：这些技能（尤其是 Hub-adapted 的 6 个）可以直接在 Hub 的 Agent 里触发使用，Hub 提供画布、图片/视频/音频/剪辑工具。
3. **手动放入 Agent 技能目录**：把技能文件夹拷进支持 skills 的 Agent（如 Codex 的 `~/.codex/skills/`、Claude 的 `~/.claude/skills/`）。本目录 `official_h3_skill/style_skills/` 已下载好全套。
4. **本项目的用法（ComfyUI + 本地 H3）**：在 Agent 端（比如我现在这样）用 `h3-prompt-writing` 格式生成最终提示词；在 ComfyUI 里：
   - T2V / I2V：填进 `MiniMaxH3ImageToVideo` 节点的 prompt 字段（fl2va 权重）。
   - R2V：填进 `MiniMaxH3ReferenceToVideo` 节点的 prompt 字段（ref2va 权重），**参考素材按连接顺序用 `<Picture 1>` `<Video 1>` `<Audio 1>` 标签指代**，与 `ref-en.txt` 格式一一对应。
   - 官方 Comfy 教程也直接引用同一份 prompt 指南：`https://docs.comfy.org/tutorials/video/minimax/minimax-h3`

### 给你的实用建议

- 你在 ComfyUI 里本地跑 H3，等于绕过了官方的 H3-Context-IR（云端自动改写提示词的模块）。本地模型对提示词结构更敏感，所以**手写/用技能生成标准结构提示词的价值比用官方 API 时更大**——这正是 h3-prompt-writing 那套格式（base-en.txt / ref-en.txt）要解决的问题。
- 8 个风格技能对你有参考价值的主要是两样：①它们沉淀的「最终 H3 prompt」写法（尤其是 handdrawn-live-video-generator 的中文 prompt 结构和 co-op 的 H3 prompt 模板）；②产品广告/3D/MV 等场景的提示词套路，可以吸收进你自己的提示词生成项目。
- 需要锚点图/定帧图时（如产品广告的 3 张锚点、拼贴的定帧），这些技能会调用图片生成工具；ComfyUI 端你可以在外面生成图后作为参考图接入，也可以让我按技能规则产出图片生成提示词，你在其他图像工具里出图。
