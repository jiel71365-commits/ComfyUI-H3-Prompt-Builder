# MiniMax H3 提示词生成引擎 · 规则库

> 版本：v1（2026-08-06）
> 依据：官方 `h3-prompt-writing` 技能（base-en.txt / ref-en.txt）＋ 手册实战案例 ＋ 8 个官方风格技能
> 用法：收到大白话需求 → 按本规则库判定模式 → 套风格规则 → 按官方结构输出 H3 提示词 → 跑自检清单

---

## 0. 处理流程（每次生成都走一遍）

1. **判定模式**：看用户有没有提供参考素材（图/视频/音频），有几张、什么角色。
2. **锁定参数**：时长（4–15s）、画幅（21:9/16:9/4:3/1:1/3:4/9:16）、风格、切镜策略；用户没说就用推荐默认值并明确告知。
3. **匹配风格规则**：需求命中第 5 节某个风格库就套用其规则；命不中就按通用规则。
4. **按官方结构输出**：基础模式用第 2 节模板，全能参考用第 3 节模板。
5. **自检**：跑第 6 节清单，全部通过才交付。

---

## 1. 模式判定表

| 模式 | 判定依据 | 一句话含义 | ComfyUI 对应 |
|---|---|---|---|
| T2VA | 无任何参考素材 | 纯文生视频 | T2V 工作流（fl2va 权重） |
| I2VA | 1 张图，明确为首帧 | 从首帧向前发展 | I2V 工作流（first_frame） |
| FL2VA | 2 张图（首+尾帧） | 补两帧之间的连续路径 | I2V 工作流（first_frame + last_frame） |
| L2VA | 1 张图，明确为尾帧 | 从推测的前序状态收敛到尾帧 | I2V 工作流（last_frame） |
| Ref2VA | 图/视频/音频混合（图≤9、视频≤3、音频≤3、总数≤12） | 多模态全能参考 | R2V 工作流（ref2va 权重） |

判定时先问清楚素材角色；如果用户只有一张图没说首尾，默认按 I2VA 首帧处理并在交付时注明。

---

## 2. 基础模式输出结构（T2VA / I2VA / FL2VA / L2VA）

### 2.1 图片对齐指令（有图才写，必须第一行，后空一行）

- I2VA：`For the target video, at 0.00 seconds into the target video, <Picture 1> (from [Shot 1]) is fully referenced.`
- FL2VA：`How the reference pictures align with the target video — Picture 1 (from Shot 1) aligns with the 0.00-second mark of the target video; Picture 2 (from Shot N) aligns with the S.SS-second mark of the target video.`（S.SS = 视频实际总时长，两位小数；N = 最后镜头编号）
- L2VA：`How the reference pictures align with the target video — <Picture 1> (from [Shot N]) aligns with the S.SS-second mark of the target video.`

### 2.2 三个核心字段（固定顺序）

```text
integrated_multimodal_description: [Shot 1] ...
overall_soundscape: ...
non_diegetic_music: ...
```

**integrated_multimodal_description（主文）**
- `[Shot 1]` 无时间戳；开头先定风格与初始构图：`[Shot 1] Cinematic, live-action, a medium-wide shot frames ...`
- 后续镜头：`[Shot 2] At 00:03.500, the camera cuts to ...`（时间严格递增、在时长内）
- 每镜头写：景别、主体外观与位置、环境与光照、动作与状态变化、运镜、台词/演唱、画面内声音
- 关键帧模式（I2VA/FL2VA/L2VA）的行文结构：
  - I2VA：首帧锚点 → 动作开始 → 连续发展 → 结果/反应
  - FL2VA：首帧状态 → 可观察的中间变化 → 差异逐渐收窄 → 尾帧状态
  - L2VA：合理前序状态 → 明确动作与过渡路径 → 尾镜逐渐收敛 → 落在尾帧
- 台词与镜头时长匹配（口型问题的根源）；跨镜头台词用 `<scenetrans>` 并写明音频延续

**overall_soundscape（环境声）**
- 1–4 句英文连续段落；概括全片环境音与物理音效（风、雨、交通、脚步、衣料、撞击、呼吸、笑声、喘气）
- 对白、演唱、画面内音乐不写在这里；全片要求完全无声才写 N/A

**non_diegetic_music（配乐）**
- 1–3 句英文；只写器乐、速度、节奏、动态变化；禁止抽象情绪词
- 没有配乐写 N/A；角色能听到的音乐属于画面内事件，写进主文

---

## 3. 全能参考模式输出结构（Ref2VA）

### 3.1 六段固定顺序

```text
subject_definitions:
summary:
retention_analysis:
detailed_description:
overall_soundscape:
non_diegetic_music:
```

### 3.2 标签体系

| 标签 | 含义 |
|---|---|
| `<Subject N>` | 可复用可见内容：人物/动物/物体/场景/服装/道具/界面/特效/风格/动作/表情 |
| `<Picture N>` | 作为具体帧/构图锚点的图（首帧、关键帧、尾帧、故事板） |
| `<Video N>` | 被直接编辑、续写或提供镜头/节奏/时间结构的原视频 |
| `<Audio N>` | 被复制或参考的音频信号 |

规则：标签一旦定义，在 summary / retention_analysis / detailed_description / 声音段保持一致含义；不能定义后不用（悬空标签）。

### 3.3 summary 任务类型（` + ` 组合，不重复）

`keyframe completion` / `reference generation` / `video editing` / `video continuation` / `audio reuse` / `audio reference`

编辑任务以 `The target video is an edited version of <Video 1>.` 开头。例：`[video editing + audio reuse]`。

### 3.4 retention_analysis 关系标记

- 可见内容：`fully_preserved` / `partially_preserved` / `attribute_transfer` / `weak_reference`
- 音频：`fully_copy` / `partially_copy` / `reference` / `weak_reference`
- 每行格式：`<Subject 1> (appears in [Shot 1], [Shot 3]): fully_preserved - 说明`

### 3.5 detailed_description

- 生成类 350–500 英文词；对话密集以完整放下台词为准；视频编辑按源视频复杂度缩放
- 先写 1–2 句风格总起句，再进 `[Shot 1]`
- 参考标签在首次出现处插入，之后沿用；镜头内写清参考内容实际出现的位置
- 不用剧情梗概；不重复静态图描述（FL2VA 要写的是两帧之间的运动路径）

---

## 4. 通用写作铁律（所有模式）

### 4.1 镜头与切镜

- 切镜必须带来新信息（主体/空间/状态/视角/时间变化）；只是距离或角度微调 → 用运镜不切镜
- 普通切镜：`the camera cuts to / the shot cuts to / transitions to / changes to / switches to`
- 用户明确要求才用 cross-dissolve / fade / wipe；MV 类场景默认只允许硬切
- 切镜时写明「切到什么景别、主体是之前哪个角色」→ 保持跨镜头一致性
- 一镜到底：全文保持单段情节描述，不用【镜头 N】结构

### 4.2 运镜词汇（类型 + 幅度 + 速度，写成自然句）

| 维度 | 可选值 |
|---|---|
| 类型 | Zoom In/Out、Push In/Pull Out、Pan Left/Right、Truck Left/Right、Tilt Up/Down、Pedestal Up/Down、Arc Shot、Tracking Shot、Static Shot、Shake Slightly/Strongly、POV、Roll Clockwise/Counterclockwise |
| 幅度 | with small amplitude / with large amplitude |
| 速度 | at slow speed / at fast speed |

例：`The camera pushes in with small amplitude at slow speed toward the folded letter in her hands.`
环绕运镜按官方建议写 truck+pan 组合，不写「环绕运镜」。

### 4.3 说话人与台词

- 说话人统一 `(S1)` `(S2)`，跨镜头不变；同时说用 `(S1,S2)`；不发声不编号
- 首次出现给身份信息（角色类型/年龄/性别/画内画外/音色/语速/口音）
- 台词：`<d>[Language] 原文</d>`，逐字保留，不翻译不改写；标准化标点（. ? !），删装饰符号
- 画外音：`says in an off-screen voiceover: <d>...</d>` + 后面必须写明对应角色嘴不动
- 跨镜头台词：两端 `<scenetrans>` + 写明音频跨切镜延续；视频结尾被截断用 `<cutoff>`
- 听不清的片段写 `[unclear]`，不要猜

### 4.4 画面可见文字

- 招牌/字幕/标题/按钮/UI 文案：英文双引号 `"..."` 逐字原样，不翻译
- 必须写原文和出现时间；禁止乱码/错拼；需要淡入淡出还是弹出要写明
- 涉及品牌/版权素材时提醒用户需授权素材，不代造 logo

### 4.5 声音设计

- 三段分工：画面内声音 → 主文；环境音 → overall_soundscape；观众级配乐 → non_diegetic_music
- 用户说「不要 BGM」→ `non_diegetic_music: N/A`；不要写「有音乐又不要 BGM」的矛盾指令
- 音乐描述给器乐/速度/节奏/动态曲线（如「前 2 秒低频建立悬疑，第 3 秒进鼓点」），不给情绪词
- 技能类默认音频政策：产品广告默认 H3 原生音频；拼贴默认只留 SFX；品牌宣传默认原生音频、字幕默认不加

### 4.6 负面清单

- 按「不要什么」逐条列出，写实可见的否定（不要字幕/水印/现代元素/动画感/廉价恐怖/跳吓）
- 负面项要具体：禁止「过度磨皮」「大幅飞入飞出」这类可执行描述

### 4.7 语言政策

- 默认按官方格式输出：**结构字段用英文，台词/歌词/画面文字保留原语言**
- 用户明确要中文 prompt（或手绘实拍类技能要求跟随输入语言）时，允许输出中文版，但保留官方字段骨架
- 交付时说明语言选择，方便用户改

### 4.8 时长、画幅与网格

- 时长 4–15s；画幅 21:9/16:9/4:3/1:1/3:4/9:16（首/尾帧模式跟随图片比例）
- ComfyUI 时长按 17 帧/块（17k+5）@24fps 对齐；切镜时间点尽量落在帧网格上
- 用户输入提示词上限 7000 字符；官方 Context-IR 产出可更长

---

## 5. 风格规则库（命中即套用）

### 5.1 极简产品广告（Apple 风）
- 触发：产品图/电商/新品发布/「苹果味儿」
- 参数默认：10s，16:9，白科技/暗边光/品牌色场/生活场景四种模板
- 文案：3–5 个英文单词、单行、≤32 字符；同一时刻只允许一行；双色（前半黑/白 + 后半产品真实主色）；禁上下两行、禁副标题位；字体感 SF Pro Display Semibold
- 视觉：负空间、真实产品动作（开盖/旋转/磁吸/滑动）驱动转场，禁白闪/粒子/假 UI 装饰
- 硬保真：产品本体颜色/材质不可改，只换背景与光
- 结构：3 张独立锚点图（主视觉/材质细节/结尾文案构图）→ 节拍分镜表 → H3 视频；禁四宫格拼板
- 音频：默认 H3 原生；不满意用 music-2.6 换 BGM 并卡点
- 负面：禁四格/分屏/拼贴/产品墙结尾、禁镜面反光舞台、禁空开场

### 5.2 3D 动画短片
- 触发：完整故事型 3D 动画
- 默认 Pixar 风：C4D+Octane 质感、2.5–3 头身 Q 版、SSS 皮肤、挤压拉伸表演
- 流程：项目简报 → 故事大纲 → 角色卡（带标签、多视图）→ 场景卡（纯环境）→ 六列镜头表（Shot ID/连续性交接/参考锚点/Hook/逐秒指令/音频轨道）→ 文字分镜 → 逐镜头视频 → 合片+BGM
- 一致性：每镜头 prompt 绑定角色卡与场景卡；身份锚点（发型/服装色/道具）逐条写
- 负面：禁写实真人、2D 平涂、塑料皮肤、僵硬摆拍
- 时长 >15s 必须逐镜头生成再拼接

### 5.3 纸艺定格动画
- 触发：科普/教育/纸艺/立体书/微观模型
- 材质：分层卡纸、可见纤维/折痕/切边/厚度、真实投影、前景遮挡、视差
- 运动：定格感（阶梯动作+小停顿+回弹+机关滑动），禁顺滑 CG/液体变形/高速环绕
- 分镜密度：15s≈4 镜、30s≈5–6 镜、60s≈7–9 镜；每镜一个知识点
- 声音：纸翻/剪刀/纸板滑动/咔嗒/胶带撕；音乐跟随题材文化（中秋→筝琶箫）
- 负面：塑料 3D、光泽 CG、写实真人、矢量平涂、赛博霓虹、玻璃金属、光滑边缘

### 5.4 半色调纸拼贴
- 触发：观点/抽象概念/一句话转视觉隐喻
- 视觉：平坦色场 + 黑白半色调剪贴 + 少量彩色卡纸 + 奶油描边 + 纸影；默认 16:9、每段约 4s
- 音频默认：只留拼贴 SFX（滑/弹/按平/轻响）；BGM、旁白、字幕默认不加，除非明确要求
- 流程：生产计划批准 → 定帧图批准 → 才生成动画；运动 = 从色纸场逐件拼装（出现→轻弹→按平→停顿→锁定）
- 负面：禁可读文字/UI/水印/logo、禁平滑数字位移、禁褪色做旧纸

### 5.5 MV / 歌词字幕
- 触发：音乐视频/歌词排版/卡点 MV（Trap/Dark-pop/Cyber-grunge 等）
- 前置：锁定歌词（用户提供即锁定，禁止改写；没有则生成原创并锁定）；锁定音乐窗口；锁定画幅
- 排版：字是空间动态图形层，不是字幕条；可被遮挡但禁遮眼/关键表情；演唱时画面文字必须逐字等于演唱歌词；每镜头一个主文字事件
- 剪辑：只硬切，禁淡入淡出；hi-hat→微震/跳帧，snare→放大/硬切/肩部下压，808→低频压屏/变形
- >15s：全局 Master Audio + 镜头表（2–5s/镜）+ 尾帧/首帧续接 + 节拍网格对齐；不打断元音中切开
- 模板字段：Vocal Line / Typography / Visual & Action / Camera & Motion / Transition Out

### 5.6 品牌宣传片
- 触发：品牌/产品/网站/App 宣传
- 默认 15–30s；素材必须可验证（官方链接/用户原文件），禁 AI 复刻 logo/字体/包装
- 结构：品牌事实表 → 来源清单 → 故事主轴（AI/SaaS、实体产品、服务、形象型）→ 帧级节拍计划（15s≈5–8 拍）→ 硬确认 → 生成
- 音频默认 H3 原生；字幕默认不加；文案/CTA 逐字写死
- 负面：禁假 HUD、玻璃卡片、装饰文字墙、未核实数据、同一种缓动贯穿全片

### 5.7 联机游戏片头
- 触发：双人游戏菜单/片头
- 流程：先出 1 张确认图（固定框架：左上玩家卡、右侧竖排菜单、底部警示胶带、Continue 高亮），批准后才写 H3 视频 prompt
- 角色：参考图只取身份锚点（脸型/发型/眼镜），按选定风格重绘
- UI：按钮单行、统一圆角/尺寸、调色板 ≤5 色、红=危险色、字体粗无衬线全大写
- 模板已存档：`style_skills/co-op-game-intro-generator/references/h3-video-prompt-template.md`

### 5.8 手绘发光动画 × 实拍
- 触发：手绘/涂鸦/蜡笔动画融合实拍、15s 变形追逐
- 输出语言跟随用户输入语言（中文输入→中文 prompt）
- 固定结构：开头句（15秒，16:9横版视频。将实拍的〇〇与手绘发光动画融合的影像。）→ 实拍空间与手机质感 → 0–3s 接触 → 3–6 → 6–10 → 10–13 → 13–15 → 手绘质感 → 相机慢半拍 → 禁止项 → 环境音
- 核心结构不可变：动画与真实手/物体接触、同一存在连续变形（保留前一形态痕迹）、相机总是慢半拍、结尾空间级变形+可爱余韵
- 质感：蜡笔/粉笔/彩铅/粉彩，线条轻抖、涂抹不均、毛边、逐帧重画感
- 负面：3DCG、毛绒玩具、均匀矢量线、平滑霓虹、恐怖怪物、巨大眼睛、裂嘴、牙齿、跳吓、突然黑屏

---

## 6. 输出自检清单（交付前逐项过）

- [ ] 模式判定正确（素材数量与角色匹配 T2VA/I2VA/FL2VA/L2VA/Ref2VA）
- [ ] 图片对齐指令（如有图）是第一行且格式正确
- [ ] 字段名与顺序完全按官方模板（三字段或六段式）
- [ ] `[Shot 1]` 无时间戳；后续镜头时间递增且在总时长内
- [ ] 运镜写成自然句；只描述看得见听得见的内容，无抽象比喻
- [ ] 台词/歌词逐字原文，`<d>[Language] ...</d>`；画外音后注明嘴不动
- [ ] 画面可见文字全部逐字写出，无乱码风险
- [ ] 声音三段不重复：主文（画面内）/ soundscape（环境）/ music（配乐）；不要 BGM 时写 N/A
- [ ] 时长与镜头数匹配（1s 说一大段话的情况已避免）
- [ ] 参考标签无悬空；编辑任务标了保留关系
- [ ] 负面清单具体可执行
- [ ] 命中风格库的规则全部应用（文案规则/音频政策/禁止项）
- [ ] 交付时注明：模式、时长、画幅、语言、参考素材如何接入 ComfyUI

---

## 7. ComfyUI 接入备注（交付提示词时附带）

- T2V / I2V：prompt 填入 `MiniMaxH3ImageToVideo`（fl2va 权重）；首帧接 first_frame、尾帧接 last_frame
- R2V：prompt 填入 `MiniMaxH3ReferenceToVideo`（ref2va 权重）；**参考素材按节点连接顺序用 `<Picture 1>` `<Video 1>` `<Audio 1>` 标签指代**
- `ref_image_size`：match（快，缩小到生成分辨率）或 max（保 2048 短边，身份保真更强，慢）
- 时长自动吸附到 17 帧/块网格；分辨率短边 768 起步，网格取 32 的倍数
- 官方教程：docs.comfy.org/tutorials/video/minimax/minimax-h3
